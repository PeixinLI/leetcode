class Solution {
public:
    vector<string> restoreIpAddresses(string s) {
        vector<string> res;
        
    }
    void find_IP(string s, vector<string>& res, int pos, int last_dot, int dot){
        if(pos == res.end() - 1 && dot == 0){
            string t.copy(s, );
        }
    
    }
};